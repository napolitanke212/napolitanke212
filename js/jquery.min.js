$(document).ready(function () {
    $('#payments-text li ul').hide().click(function (e) {
        e.stopPropogation();
    })
        $('#payments-text li ul').toggle(function () {
            $(this).fund('ul').slideDown();
        }, function () {
            $$(this).fund('ul').slideUp();
        }
        )
})
    